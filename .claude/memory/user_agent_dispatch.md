---
name: user-agent-dispatch
description: User prefers agent-based task execution — designs specs, then dispatches each issue to agents
metadata:
  type: user
---

User workflow: design comprehensive specs with module breakdown and issue lists, then execute each issue via Claude Code agents. Prefers clear issue boundaries so each agent task is self-contained. Does not want direct implementation during planning phase — explicitly separates planning from execution.
